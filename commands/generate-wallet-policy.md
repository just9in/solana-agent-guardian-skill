# Command: generate-wallet-policy

Generate a conservative JSON wallet policy manifest compatible with `schemas/agent-wallet-policy.schema.json`.

Default: deny unknown programs, require simulation, require approval for new recipients, block authority changes, block full-balance transfers, and separate policy admin from agent runtime.
